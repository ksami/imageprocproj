function translation = findTranslationMatrix(img, translatedImg)
% Given an img and its translated result such that
% [translatedImg] = [translation] * [img]
% find [translation]
% Inputs : [double] img, [double] translatedImg
% Outputs: [double] translation